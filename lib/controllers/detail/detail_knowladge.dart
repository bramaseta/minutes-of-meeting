part of '../controllers.dart';

class DetailKnowladgeController extends GetxController {
  // List<GaleryData> listGalery = [];

  // Future<void> getListGaleryByIdArticle(BuildContext context, ArticleData item) async {
  //   WrapResponse? resData = await Api().GET('$API_GALERY?article_id=${item.idArticle}', context);
  //   if (resData?.data['data'] != null) {
  //     List<GaleryData> listGaleryData = [];
  //     for (var element1 in resData?.data['data']) {
  //       listGaleryData.add(GaleryData.fromJson(element1));
  //     }
  //     listGalery = listGaleryData;
  //     update();
  //   }
  // }

  // Future<void> initPage(BuildContext context, ArticleData item) async {
  //   await getListGaleryByIdArticle(context, item);
  // }
}